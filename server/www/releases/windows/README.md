Place release installers here on the server:

- `/var/www/updesk/releases/windows/updesk-<version>.exe`

This path is intentionally separate from `/ws/` and can be served by nginx as a static file tree.
